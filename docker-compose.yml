services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portfolio-nginx
    restart: unless-stopped
    networks:
      - portfolio-network

networks:
  portfolio-network:
    external: true